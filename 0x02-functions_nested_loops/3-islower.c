#include "main.h"
#include <ctype.h>

/**
  * int _islower(int c) - checks if value is lowercase
  * int main(void) - calls functiov
  * Return: returns 1
  */

int main(void)
{
	int c;

	int _islower(int c);
}

int _islower(int c)
{
	if (islower())
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
